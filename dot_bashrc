#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls -la --color=auto'

export EDITOR=vim
export VISUAL=vim
export TERM=xterm-256color

PS1='[\u@\h \W]\$ '
